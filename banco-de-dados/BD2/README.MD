# 🗄️ Banco de Dados – BD2

> Exercícios, consultas SQL, revisões e exemplos práticos desenvolvidos na disciplina de **Banco de Dados II**.

---

## 📋 Conteúdo da Pasta

Esta pasta contém arquivos **SQL** com consultas e exercícios relacionados aos seguintes tópicos:

✔ Consultas simples  
✔ Consultas com `SELECT`  
✔ `JOIN` e subconsultas  
✔ Operações `INSERT`, `UPDATE` e `DELETE`  
✔ Revisão de `DML`  
✔ `HAVING`, `GROUP BY` e funções de agregação  
✔ `VIEW` e abstração de dados  

---

## 📁 Estrutura dos Arquivos

| Arquivo | Tema |
|---|---|
| `AULAVIEW.BD.sql` | Criação e uso de VIEW |
| `EXERCICIO06_SELECT.sql` | Consultas básicas com SELECT |
| `EXERCICIO_REVISAO_DML.sql` | Revisão de comandos DML |
| `SUBCONSULTASELET_SELECTINTO.sql` | Subconsulta com SELECT INTO |
| `SUBCONSULTASELETSELECTFROM_BD.sql` | Subconsulta com SELECT FROM |
| `SUBCONSULTASELETSELECTINTO_BD.sql` | Inserção via subconsulta |
| `SUBCONSULTASELET.BD.sql` | Exemplos gerais de subconsultas |
| `SUBCONSULTASFROM2_BD.sql` | Subconsulta FROM (nível intermediário) |
| `SUBCONSULTASFROM_BD.sql` | Variantes de subconsultas FROM |
| `SUBCONSULTASHAVING_BD.sql` | Uso de HAVING e agregações |
| `SUBCONSULTASHAVING2_BD.sql` | HAVING + filtros |
| `SUBCONSULTASHAVING_REVISAOAPROVA_BD.sql` | Exercício de revisão com HAVING |
| `SUBCONSULTA_UPDATE_DELETE.sql` | Atualização e remoção com subconsultas |

---

## 🧠 Conceitos Trabalhados

📌 **Subconsultas (`Subqueries`)**  
📌 **Operações DML (`INSERT`, `UPDATE`, `DELETE`)**  
📌 **Operações DQL (`SELECT`)**  
📌 **Funções de agregação (`COUNT`, `AVG`, `SUM`, etc.)**  
📌 **Views**  
📌 **Filtros com HAVING**  
📌 **Agrupamento com GROUP BY**  

---

## 📝 Pré-requisitos

Para executar os scripts é necessário um SGBD compatível com SQL padrão:

- PostgreSQL
- MySQL/MariaDB
- SQL Server
- SQLite *(com pequenas adaptações)*

---

## 🎯 Objetivo da Disciplina

✔ Entender conceitos intermediários e avançados de SQL  
✔ Aprofundar subconsultas e manipulação de dados  
✔ Exercitar modelagem lógica aplicada ao contexto relacional  

---

## 📎 Observação

Este material foi desenvolvido com fins **acadêmicos e educacionais**.

